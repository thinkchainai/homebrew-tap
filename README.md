# MCPBundles Homebrew Tap

Official Homebrew tap for the MCPBundles CLI.

```sh
brew tap thinkchainai/tap
brew install mcpbundles
```

After installation:

```sh
mcpbundles --help
mcpbundles call --help
```
